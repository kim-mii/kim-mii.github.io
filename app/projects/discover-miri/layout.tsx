import { createMetadata } from '../../seo';

export const metadata = createMetadata({ title: 'Discover Miri — Tourism App Product Design | Kimberly Toh', description: 'A tourism discovery app designed for Miri City Council, shaping research, user journeys, product structure, UI, and a reusable component system.', path: '/projects/discover-miri/', image: '/assets/projects/discover-miri/cover.jpg' });

export default function Layout({ children }: Readonly<{ children: React.ReactNode }>) { return children; }
