import { createMetadata } from '../../seo';

export const metadata = createMetadata({ title: 'Kuching Marathon 2016 — Event Website Design | Kimberly Toh', description: 'The official Kuching Marathon 2016 website, designed to turn event interest into registration through a clear, responsive runner journey.', path: '/projects/kuching-marathon-2016/', image: '/assets/projects/kuching-marathon-2016/cover.jpg' });

export default function Layout({ children }: Readonly<{ children: React.ReactNode }>) { return children; }
