import { createMetadata } from '../../seo';

export const metadata = createMetadata({ title: 'Revenue Harvest — Corporate Website Design | Kimberly Toh', description: 'A corporate website redesign for Revenue Harvest, creating a credible digital presence around growth, investor confidence, products, and services.', path: '/projects/revenue-harvest/', image: '/assets/projects/revenue-harvest/revenue-harvest-cover.jpg' });

export default function Layout({ children }: Readonly<{ children: React.ReactNode }>) { return children; }
