import { createMetadata } from '../../seo';

export const metadata = createMetadata({ title: 'Revenue Investor — Investor Relations App Design | Kimberly Toh', description: 'A mobile investor relations app that makes share information, reports, announcements, company details, and financial documents easier to access.', path: '/projects/revenue-investor/', image: '/assets/projects/revenue-investor/cover.jpg' });

export default function Layout({ children }: Readonly<{ children: React.ReactNode }>) { return children; }
